# ASI_2026_PJATK
Repository created for university project. Goal is to create an artificial intelligence to predict NBA games outcome.

## Local development setup

The project can be run locally using Python virtual environment.

Create and activate virtual environment:

```bash
python3 -m venv .venv
source .venv/bin/activate
DOC
